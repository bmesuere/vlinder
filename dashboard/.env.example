# Base URL for the VLINDER API. Optional: falls back to the production API
# (https://mooncake.ugent.be/api) when unset, so the samba-share production
# deploy needs no configuration.
VITE_API_URL=https://mooncake.ugent.be/api
